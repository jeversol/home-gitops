apiVersion: v1
kind: Service
metadata:
  name: audiobookshelf
  namespace: media
  labels:
    app.kubernetes.io/name: audiobookshelf
spec:
  type: ClusterIP
  ports:
    - port: 13378
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: audiobookshelf
