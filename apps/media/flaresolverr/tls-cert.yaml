apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: flaresolverr-tls
  namespace: media
spec:
  secretName: flaresolverr-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  privateKey:
    rotationPolicy: Never
  dnsNames:
    - flaresolverr.i.recompiled.org