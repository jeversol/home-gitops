apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tautulli-pvc
spec:
  storageClassName: democratic-iscsi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

