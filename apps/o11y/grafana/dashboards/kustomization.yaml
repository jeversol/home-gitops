apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: o11y

configMapGenerator:
  - name: grafana-dashboards
    files:
      - pod-resources.json

generatorOptions:
  disableNameSuffixHash: true