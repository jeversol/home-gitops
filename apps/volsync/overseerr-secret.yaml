apiVersion: v1
kind: Secret
metadata:
  name: overseerr-volsync-restic
  namespace: media
type: Opaque
stringData:
  RESTIC_REPOSITORY: s3:https://nas.i.recompiled.org:3900/volsync/config-overseerr-0
  RESTIC_PASSWORD: ${RESTIC_PASSWORD}
  AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
  AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
  AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION}
