apiVersion: v1
kind: Secret
metadata:
  name: influxdb-volsync-restic
  namespace: o11y
type: Opaque
stringData:
  RESTIC_REPOSITORY: s3:https://nas.i.recompiled.org:3900/volsync/influxdb
  RESTIC_PASSWORD: ${RESTIC_PASSWORD}
  AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
  AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
  AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION}
