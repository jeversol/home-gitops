apiVersion: v1
kind: Secret
metadata:
  name: bambu-spoolman-config-volsync-restic
  namespace: spoolman
type: Opaque
stringData:
  RESTIC_REPOSITORY: s3:https://nas.i.recompiled.org:3900/volsync/bambu-spoolman-config
  RESTIC_PASSWORD: ${RESTIC_PASSWORD}
  AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
  AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
  AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION}
