apiVersion: apps/v1
kind: Deployment
metadata:
  name: bambu-spoolman
  namespace: spoolman
  labels:
    app: bambu-spoolman
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: bambu-spoolman
  template:
    metadata:
      labels:
        app: bambu-spoolman
    spec:
      containers:
        - name: bambu-spoolman
          image: ghcr.io/jeversol/bambu-spoolman:personal@sha256:85b24105c85fab4f9eef7027618fea047e892a55e7605d0c5274c70c47d33953
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
          envFrom:
            - configMapRef:
                name: bambu-spoolman-config
            - secretRef:
                name: bambu-spoolman-printer
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              cpu: 50m
              memory: 256Mi
            limits:
              memory: 512Mi
          volumeMounts:
            - name: config
              mountPath: /config
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: bambu-spoolman-config
