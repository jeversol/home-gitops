apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bambu-spoolman-tls
  namespace: spoolman
spec:
  secretName: bambu-spoolman-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - bambu-spoolman.i.recompiled.org
