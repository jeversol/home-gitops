apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bambu-spoolman-config
  namespace: spoolman
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
