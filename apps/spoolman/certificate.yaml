apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: spoolman-tls
  namespace: spoolman
spec:
  secretName: spoolman-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - spoolman.i.recompiled.org