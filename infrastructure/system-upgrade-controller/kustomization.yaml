apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - system-upgrade-controller.yaml
  - talos-config-secret.yaml
  - plans.yaml

commonLabels:
  app.kubernetes.io/name: system-upgrade-controller