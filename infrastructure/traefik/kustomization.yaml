apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
patches:
  - target:
      kind: ServiceAccount
      name: traefik
      namespace: traefik
    patch: |
      - op: replace
        path: /automountServiceAccountToken
        value: true