---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: basic-auth
  namespace: auth
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: basic-auth
  updatePolicy:
    updateMode: "Off"  # Recommendation-only mode
