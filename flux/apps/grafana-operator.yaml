apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: grafana-operator
  namespace: flux-system
spec:
  interval: 30m
  releaseName: grafana-operator
  targetNamespace: o11y
  chart:
    spec:
      chart: grafana-operator
      version: "v5.18.0"  # renovate: datasource=helm depName=grafana-operator registryUrl=https://grafana.github.io/helm-charts/
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: flux-system