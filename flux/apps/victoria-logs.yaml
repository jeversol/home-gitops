---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: victoria-logs
  namespace: flux-system
spec:
  interval: 30m
  releaseName: victoria-logs
  targetNamespace: o11y
  dependsOn:
    - name: victoria-metrics-operator
      namespace: flux-system
  driftDetection:
    mode: warn
    ignore:
      - paths: ["/spec/replicas"]
        target:
          kind: Deployment
      - paths: ["/spec/replicas"]
        target:
          kind: StatefulSet
  chart:
    spec:
      chart: victoria-logs-single
      version: "0.11.4"  # renovate: datasource=helm depName=victoria-logs-single registryUrl=https://victoriametrics.github.io/helm-charts/
      sourceRef:
        kind: HelmRepository
        name: victoria-metrics
        namespace: flux-system
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    nameOverride: logs
    fullnameOverride: logs
    server:
      retentionPeriod: "14d"
      persistentVolume:
        enabled: true
        storageClass: "longhorn"
        size: 30Gi
      resources:
        requests:
          cpu: "250m"
          memory: "256Mi"
        limits:
          cpu: "500m"
          memory: "512Mi"
      vmServiceScrape:
        enabled: true
        